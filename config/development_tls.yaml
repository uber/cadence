services:
  frontend:
    rpc:
      tls:
        enabled: true
        certFile: config/credentials/keytest.csr
        keyFile: config/credentials/keytest

  matching:
    rpc:
      tls:
        enabled: true
        certFile: config/credentials/keytest.csr
        keyFile: config/credentials/keytest

  history:
    rpc:
      tls:
        enabled: true
        certFile: config/credentials/keytest.csr
        keyFile: config/credentials/keytest

clusterGroupMetadata:
  clusterGroup:
    cluster0:
      tls:
        enabled: true
