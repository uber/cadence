global:
  scrape_interval: 5s
  external_labels:
    monitor: 'cadence-monitor'
scrape_configs:
  - job_name: 'prometheus' 
    static_configs: 
      - targets: ['localhost:9090', 'localhost:8000', 'localhost:8001','localhost:8002', 'localhost:8003' ] ## address to scrape
