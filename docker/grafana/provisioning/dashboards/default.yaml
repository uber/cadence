apiVersion: 1

providers:
  - name: Cadence-Basic # A uniquely identifiable name for the provider
    folder: Cadanece
    type: file
    options:
      path:
        /etc/grafana/provisioning/dashboards/
